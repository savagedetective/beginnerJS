console.log('I work');

/* eslint-disable */

const name = 'ritch';

const middle = "middle name";

const last = `last name`;

const hello = `Hello my name is ${name}. Interpolation is fun`;

const person = {
    first: "ritchie",
    last: 'last name',
    age : 22222
};